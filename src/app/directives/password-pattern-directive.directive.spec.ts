import { PasswordPatternDirectiveDirective } from './password-pattern-directive.directive';

describe('PasswordPatternDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordPatternDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
