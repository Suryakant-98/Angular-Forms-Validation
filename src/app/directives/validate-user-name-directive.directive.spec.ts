import { ValidateUserNameDirectiveDirective } from './validate-user-name-directive.directive';

describe('ValidateUserNameDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateUserNameDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
